import Button from '@/components/Button';
import Input from '@/components/Input';

export default function Home() {
  return (
    <main>
      <div>
        <Input />
        <Button label={'Post'} />
      </div>
    </main>
  );
}
